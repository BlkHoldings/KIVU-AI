export const metadata = {
  title: 'Member Directory',
  description: 'Discover talented AI builders, founders, and innovators from across Africa.',
};

export default function MembersLayout({ children }) {
  return children;
}
